# Interactive Playground

<script>window.location.replace('./playground/index.html');</script>

→ **[Open the Declart Playground](playground/index.html)** — no installation required

Edit TOML declarations and see your diagrams rendered live in the browser.
All 8 diagram kinds are available, with default and monochrome themes.

> **Note:** The playground requires HTTP to load the WASM module.
> Use `mdbook serve docs/` for local testing rather than opening the file directly.
